package bingmclobby.Timmety.Utils;

import org.bukkit.entity.Player;

import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.UUID;

public class PlayerManagerUtil {

    public static int getTotaalgewonnen(Player player) {
        UUID uuid = player.getUniqueId();
        return 0;
    }
    public static int getTotaalGespeeld(Player player) {
        UUID uuid = player.getUniqueId();
        return 0;
    }
    public static int getTotaalItems(Player player) {
        UUID uuid = player.getUniqueId();
        return 0;
    }
    public static int getGemiddeldItems(Player player) {
        UUID uuid = player.getUniqueId();
        return 0;
    }
    public static int getTotaalPlaytime(Player player) {
        UUID uuid = player.getUniqueId();
        return 0;
    }
    public static int getGemiddeldePlaytime(Player player) {
        UUID uuid = player.getUniqueId();
        return 0;
    }
    public static Date getEersteGame(Player player) {
        UUID uuid = player.getUniqueId();

        Date now = new Date();

        return now;
    }
    public static String getRank(Player player) {
        UUID uuid = player.getUniqueId();
        return "Player";
    }

}
